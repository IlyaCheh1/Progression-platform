---
description: Go APIs, workers, contracts, and HTTP/command handlers
globs: apps/**/*.go,packages/contracts/**/*.go,scripts/excel-import/**/*.go,apps/support-chat/api/**/*
alwaysApply: false
---

# Backend and API rules

- Validate and normalize all external input at the boundary.
- Authenticate first, then authorize access to the specific resource and action.
- Keep transport/handler code thin; place business rules in testable domain packages.
- Return stable, documented error codes and avoid leaking internal details.
- Writes that may retry must be idempotent (command fingerprint / domain idempotency key).
- Persist state with transactional outbox; consumers use inbox dedup — see root `AGENTS.md` invariants.
- Use transactions when multiple writes must succeed or fail together.
- Add timeouts, cancellation, bounded retries, and backoff for external calls where appropriate.
- Emit structured logs and useful correlation identifiers without logging secrets or sensitive payloads.
- Define pagination, filtering, ordering, rate limits, and maximum payload sizes for collection endpoints.
- Update OpenAPI/JSON schemas, Go contracts, and tests together.
- Quality gates: in each touched Go module run `go test ./...`, `go vet ./...`, `go build ./...` (support-chat: `make test` / `make build` acceptable).
