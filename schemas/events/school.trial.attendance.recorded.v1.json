{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "school.trial.attendance.recorded.v1",
  "type": "object",
  "properties": {
    "eventType": { "const": "school.trial.attendance.recorded.v1" },
    "payload": {
      "type": "object",
      "required": ["studentId", "sessionId"],
      "properties": {
        "studentId": { "type": "string" },
        "sessionId": { "type": "string" },
        "bookingId": { "type": "string" }
      }
    }
  }
}
