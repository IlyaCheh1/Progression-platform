{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "school.trial.booking.created.v1",
  "type": "object",
  "properties": {
    "eventType": { "const": "school.trial.booking.created.v1" },
    "payload": {
      "type": "object",
      "required": ["bookingId", "sessionId"],
      "properties": {
        "bookingId": { "type": "string" },
        "sessionId": { "type": "string" },
        "leadId": { "type": "string" },
        "studentId": { "type": "string" }
      }
    }
  }
}
