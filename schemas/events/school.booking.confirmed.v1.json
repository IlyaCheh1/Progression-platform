{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "school.booking.confirmed.v1",
  "type": "object",
  "properties": {
    "eventType": { "const": "school.booking.confirmed.v1" },
    "payload": {
      "type": "object",
      "required": ["bookingId", "sessionId"],
      "properties": {
        "bookingId": { "type": "string" },
        "sessionId": { "type": "string" }
      }
    }
  }
}
